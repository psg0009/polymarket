# Streamlit Cloud's `requirements.txt`. Deliberately minimal so it works on
# Python 3.14 without compiling Rust + cmake. The dashboard talks to Turso
# directly via the HTTP API (`polyclaude/ui/turso_http.py`), so no
# sqlalchemy-libsql / libsql-experimental needed here.
#
# The agent (which DOES need the full stack) installs from
# `requirements-agent.txt` in the GitHub Actions workflows.

httpx>=0.27
pydantic>=2.7
streamlit>=1.36
pandas>=2.2
matplotlib>=3.9
