# Full agent dependency set, used by the GitHub Actions workflows that run
# `polyclaude scan` / `trade` / `reconcile`. Includes libsql-experimental
# (Rust-built, only has wheels for Python 3.11) plus all the trading and
# NLP plumbing.

py-clob-client>=0.17
anthropic>=0.40
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
sqlalchemy-libsql>=0.1
libsql-experimental>=0.0.53
tenacity>=8.4
python-dotenv>=1.0
feedparser>=6.0
structlog>=24.1
vaderSentiment>=3.3
rich>=13.7
click>=8.1
